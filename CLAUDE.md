# プロジェクト: [プロジェクト名]

## 種別: アプリケーション開発

## セッション開始ルール
- このプロジェクトの作業は Plan Mode（Shift+Tab×2 or /plan）から開始すること
- Plan で実装計画を作成 → コンテキストクリア → きれいな状態で実装する
- コンテキスト管理の詳細は autonomous-rules.md の「8. コンテキスト管理ルール」に従う

## プロジェクト概要
<!-- このアプリが何をするものか -->
私はよく遠方に出かけることがあります。主な移動手段は車です。
このアプリをインストールしていると、自動的にGPSから自分の位置情報を常に記録し、私の移動経路を記録するアプリを作りたいです。
欲しい要素は以下
・Google map APIなどを用いた地図機能と、その中で自分が通った経路を線上でしめす
・10分以上同じ位置に立ち止まっている(GPSに大きな動きがない)場合、その箇所に留まっていると判断し、その留まっている場所にピンを差して記録する(ピンにはその場所のお店情報などを紐付ける)
・iOSのカレンダーと同期することが可能であり、いつどこにいったのかを管理できるようにする
・dailyで移動記録を残し、この移動記録はGoogle Driveと同期することが可能であり、エクスポートすることが可能になっている(設定によってローカルではなく自動的にGoogle Driveへの同期が可能)
・アプリ内にDBを持ち、DBはローカルで保持する
　・DBのテーブルには[日付(主キー)、移動ルート、ピン留めした位置、スタートと終了時間、総移動距離(km)]を保有し、ピン留めした位置についてはそのお店や位置のリンクを載せる
・設定で以下が可能
- カレンダー同期
- 常時同期、トリガー同期
(トリガー同期にした場合は、画面内にボタンを作り、そのボタンを押下することで記録、停止が可能。常時同期の場合、「自宅」で停滞している場合には記録を中止する)
- 自宅設定(自宅位置を登録することで、自宅にいる場合には自動的に記録ストップ、ログを開始しない。自宅を離れたら、常時同期をonにしている場合に記録を開始する)
- エクスポート(CSV)
 - エクスポートするデータはDBに収納しているデータをそのまま出力する
- 出力自動同期先の設定(Google Drive/DropBoxほかクラウド型のストレージ保存先を設定可能)
- 出力自動同期のon/off(onの場合は、記録を中止した時点でGoogle Driveなどのクラウドストレージに「GPSログ」のフォルダを作成し、配下に「日付」フォルダを作成し、その配下にCSVファイルにてDBデータを出力する)
- DBクリア機能
　- 指定の日付を選択すると、その日付のデータを削除することができる
- DB自動消去の有無(DBに1GB以上のデータが蓄積されている場合に、古い日付データから削除を行う)

## 技術スタック
- 言語:
- フレームワーク:
- ビルドツール:
- テスト:
- デプロイ先:

細かいスタックはわからないが、iOSのスマホアプリを作りたい。基本自分専用だが、有効であれば販売や広告月で無料配布などを行いたい。

## ディレクトリ構成
```
src/          - ソースコード
tests/        - テストコード
```

私はデイレクトリ構成はわからないのでお任せします

## 開発コマンド
```bash
npm install     # 依存関係のインストール
npm run dev     # 開発サーバー
npm run build   # ビルド
npm test        # テスト
```
コマンドもお任せします

## ブランチ戦略
- main: 本番
- feature/*: 機能開発

ブランチ戦略もお任せします

## 注意事項
常時起動の場合、バッテリー消費が多くなり過ぎることが懸念としてあります。
この懸念が払拭できるのであれば、その手段をよく検討して進めてください。
<!-- このプロジェクト固有の注意点 -->

